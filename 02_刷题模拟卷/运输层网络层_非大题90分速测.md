# 运输层与网络层：非大题90分速测

> 用途：检测选择、判断、填空、短简答是否达到90分稳定水平。  
> 限时建议：15-20分钟。  
> 过关标准：20题至少18题正确；错题订正后必须闭卷复答正确。

---

## 题目

### 1. TCP/UDP（判断）
UDP 是无连接、面向报文、尽最大努力交付的传输层协议。（ ）

### 2. 端口与分用（单选）
TCP 连接通常由哪一组信息唯一标识？（ ）

A. 源IP、目的IP  
B. 源端口、目的端口  
C. 源IP、源端口、目的IP、目的端口  
D. MAC地址、IP地址、端口号

### 3. RDT（填空）
可靠数据传输常用的基本机制包括序号、确认、超时重传和 ________。

### 4. GBN/SR（单选）
关于 GBN 和 SR，正确的是（ ）。

A. GBN 缓存乱序帧，SR 丢弃乱序帧  
B. GBN 累计确认且通常接收窗口为1，SR可缓存乱序帧并逐一确认  
C. GBN 和 SR 的接收窗口都必须为1  
D. SR 的窗口上限是 `2^n - 1`

### 5. Karn算法（判断）
Karn算法的核心思想是：不使用重传报文段的RTT样本来更新SRTT。（ ）

### 6. SACK（填空）
SACK 的作用是让接收方告诉发送方哪些 ________ 已经收到，从而减少不必要重传。

### 7. 零窗口（单选）
TCP 零窗口后，发送方避免死锁的机制是（ ）。

A. 慢开始  
B. 持续计时器和零窗口探测  
C. ARP广播  
D. RIP更新

### 8. 握手挥手（判断）
SYN 和 FIN 即使不携带数据，也各占用1个序号。（ ）

### 9. 数据报与虚电路（单选）
关于数据报网络与虚电路网络，正确的是（ ）。

A. 数据报网络通信前必须建立连接  
B. 虚电路网络建立后，分组沿虚电路转发  
C. 虚电路网络每个分组都完全独立选路  
D. 二者都不需要路由器维护任何状态

### 10. 转发与路由（填空）
________ 是对单个分组查表选择输出端口；________ 是生成和维护路由表的过程。

### 11. 直接与间接交付（判断）
目的主机不在同一网段时，源主机会把链路层帧交给默认网关，这是间接交付。（ ）

### 12. IP与MAC（单选）
主机访问异地服务器时，IP数据报目的IP是服务器IP，以太网帧目的MAC通常是（ ）。

A. 服务器MAC  
B. 默认网关MAC  
C. DNS服务器MAC  
D. 广播MAC

### 13. 特殊IP（单选）
DHCP客户端尚未获得IP地址时，常用源IP是（ ）。

A. 127.0.0.1  
B. 0.0.0.0  
C. 255.255.255.255  
D. 192.168.1.255

### 14. DHCP（填空）
DHCP常用UDP端口：服务器端口为 ________，客户端端口为 ________。

### 15. ICMP（判断）
ICMP 封装在 IP 数据报中，Ping 使用 ICMP 请求/应答报文。（ ）

### 16. NAT（简答）
写出 NAT 的两个主要作用。

### 17. RIP计时器（填空）
RIP 常见计时器：周期更新约 ________ 秒，超时约 ________ 秒，垃圾回收约 ________ 秒。

### 18. OSPF/BGP（单选）
OSPF 与 BGP 的正确描述是（ ）。

A. OSPF是路径向量，BGP是链路状态  
B. OSPF是IGP链路状态协议，BGP是EGP路径向量协议  
C. OSPF和BGP都以跳数作为唯一度量  
D. BGP只在单个自治系统内部使用

### 19. IPv6（判断）
IPv6地址长度为128位，路由器不负责分片，过大分组由源主机处理。（ ）

### 20. IPv4首部（多选）
IPv4首部中与转发/分片强相关的字段包括（ ）。

A. TTL  
B. 协议字段  
C. MF标志和片偏移  
D. 首部校验和

