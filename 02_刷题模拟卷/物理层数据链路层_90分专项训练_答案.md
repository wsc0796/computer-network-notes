# 物理层数据链路层_90分专项训练_答案

## 物理层答案

1对。2对。3错，半双工不能同时。4对。5错，基带信号可模拟也可数字。6对。7对。8对。9对。10错，集线器不看MAC。11对。12错，香农与W和S/N有关。13对。14对。15错，ADSL非对称。16对。17对。18对。19对。20错，可靠传输是上层问题。

21：1翻转、0不翻转，所以101100对应翻转/不翻转/翻转/翻转/不翻转/不翻转。22对。23对。24：NRZ更容易丢同步。

25：16-QAM每码元4bit，`2400×4=9600bps`。26：8-PSK每码元3bit，`12000/3=4000Baud`。27：每码元4bit，`M=16`。28：64-QAM每码元6bit，`3000×6=18kbps`。

29：`2×4000×3=24kbps`。30：`2×3000×4=24kbps`。31：30dB->1000，`3000×log2(1001)≈30kbps`。32：20dB->100，`4000×log2(101)≈26.6kbps`。33：奈氏12kbps，香农约30kbps，取12kbps。34：`16000=2×2000×log2M`，`M=16`。

35：发送1.2ms，传播0.5ms，总1.7ms。36：发送时延8ms，3跳存储转发24ms。37：首包24ms，后续4个每8ms，总56ms。38：`2,000,000/2e8=0.01s=10ms`。

39 FDM。40 WDM。41 TDM。42 该用户发送0（若约定+1为1，-1为0）。

## 数据链路层答案

43对。44错，透明不是加密。45错，CRC检错不纠错。46对。47对。48错。49对。50对。51对。52对。53对。54对。55错。56对。57对。58对。59错。60错。61对。62对。63对。64错，最大4。65错，集线器整体一个冲突域。66对。67对。

68：`011111010`。69：`01111110`。70：插入0。71：对ESC也转义，即前面再加ESC。

72：除数1011，余数111，发送串`110101111`。73：余数010。74：`10011`。75：检测到差错。

76：填充6B，帧长64B。77：`6+6+2+100+4=118B`。78：1518B。79：72B。

80：`Lmin=2dR/v=2500bit=312.5B`，至少313B。81：`dmax=512×2e8/(2×1e7)=5120m`。82：发送5.12μs，往返传播10μs，不足。83：争用期需额外覆盖设备往返/检测相关时延，按题意加入2×10μs。84：保证发送方在发送期间能检测最远端碰撞。85：端到端往返传播时延窗口。

86：`k=4`，r为0到15。87：`7×51.2=358.4μs`。

88：A发B：学A->1，B未知泛洪；B回A：学B->2，A已知转发端口1。89：端口1可学习A和B；B发A且目的也在端口1时过滤。90：学习A->3，覆盖旧A->1。

91：1个冲突域，1个广播域。92：4个冲突域，1个广播域。93：2个广播域。

94：查询B的MAC。95：查询默认网关MAC。96：Discover、Offer、Request、ACK。97：客户端68，服务器67。98：DHCP Relay。

99：7。100：4。101：重传2、3、4。102：只重传2。
