# 最终独立审查报告

> 审查时间：2026-06-21
> 审查范围：运输层与网络层 6 小时计划、执行清单、验收卷、答案、资料审计、覆盖矩阵、自动校验脚本。
> 结论：无阻断性错误，当前版本可直接执行。

---

## 阻断性错误

无。

已重点复核：

- 验收卷总分为 100 分。
- 运输层为 50 分，网络层为 50 分。
- 题号 1-13 无重复。
- 试卷与答案题号一一对应。
- 预计答题时间合计 60 分钟。
- 订正时间为 15 分钟。
- 6 小时计划总时长为 360 分钟，无重叠、无遗漏。
- VLSM 已补完整例题，不再使用“等长子网变体替代”。
- RTT/RTO 已按 RFC 6298 顺序说明：先用旧 SRTT 计算 RTTVAR，再更新 SRTT，最后计算 RTO。
- 三次握手表述已删除“服务器收到最终 ACK 后才分配资源”的不准确说法。

---

## 重要错误

无当前阻断执行的问题。

仍需如实保留的风险：

1. 网络层学习通 22 张截图只完成 `contact_sheet` 和 2 张原图核验，剩余 20 张未逐张核验。因此资料审计中保持 `PARTIAL`，不能标 `FULL`。
2. VLSM 已有完整练习材料，但本校直接考试证据仍弱，因此覆盖矩阵中只标为“资料够练习”，不标为高频。
3. T09/T10/T11 等重复候选文件未通过 SHA-256 完全一致验证，因此没有标为 `DEDUPLICATED`。

---

## 一般问题

1. 部分范围外文件仍保留旧术语 `RTTs/RTTd`，但不属于本次允许修改范围，未处理。
2. 网络层学习通截图缺少逐题 Markdown 对照文件，后续若要进一步提高准确性，应单独做图片逐题提炼。

---

## 已验证正确内容

### 时间

```text
运输层复习：125分钟
网络层复习：140分钟
休息：10分钟
自检缓冲：10分钟
闭卷验收：60分钟
订正复盘：15分钟
合计：360分钟
```

### 试卷

```text
运输层：50分
网络层：50分
总分：100分
预计答题时间：60分钟
订正时间：15分钟
```

### 关键计算

- TCP 数据段：`seq=1200`，数据 `350` 字节，`ack=1550`。
- SYN 不携带数据仍占 1 个序号：`seq=4000` 后 `ack=4001`。
- FIN 不携带数据仍占 1 个序号：`seq=8000` 后 `ack=8001`。
- RTTVAR：`25 ms`。
- SRTT：`65 ms`。
- RTO：`165 ms`。
- CIDR：`172.16.88.150/18` 属于 `172.16.64.0/18`，广播地址 `172.16.127.255`，可用主机 `16382`。
- VLSM：`192.168.10.0/24` 按 `/25, /26, /27, /28` 分配，不重叠。
- IP 分片：数据总长 `4980`，分片数据长度 `1280,1280,1280,1140`，偏移 `0,160,320,480`。

---

## 自动校验结果

`python analysis/verify_round2.py`

```text
PASS: All hard checks passed
Errors: 0
Warnings: 0
```

校验项包括：

- 总分、运输层分数、网络层分数。
- 题目预计时间总和。
- 卷首答题时间和订正时间。
- 题号唯一性。
- 试卷与答案题号对应。
- 计划时间总和、重叠、遗漏。
- 精确相对路径存在性。
- RTT/RTO 关键数值。
- IP 分片关键数值。
- VLSM 必做时例题文件存在。

---

## 是否可以直接执行

可直接执行。
