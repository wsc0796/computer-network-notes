# 06_空白默写 · 数据链路层

## 1. 三个基本问题

```
① ______：在IP数据报前后添加首部和尾部
② ______：数据不误判为控制信息（字节填充/零比特填充）
③ ______：CRC检错
```

[ ] 过关

## 2. CSMA/CD

```
争用期 = ___τ = ______（往返传播时延）
最短帧长 = ______ × ______ = 512bit = ______B
10Mbps争用期 = ______μs
100Mbps争用期 = ______μs

保证"发送期间能检测到冲突"（帧发送时间 ≥ 2τ）的方法：
① 固定速率时：减小______（网络跨距）或增大______（最短帧长）
② 固定跨距时：降低______（数据传输速率）
```

[ ] 过关

## 3. 冲突域 vs 广播域

```
集线器：冲突域___个, 广播域___个
交换机：冲突域___个/端口, 广播域___个（默认）
路由器：冲突域___个/接口, 广播域___个/接口
VLAN：在______上隔离广播域
```

[ ] 过关

## 4. CRC计算

```
生成多项式 G(x)=x⁴+x+1 → 二进制比特串：______
数据后补___个0
模2除（XOR, 不进位不借位）
余数为0 → ______（未检出差错/检测出差错）← 不能说"无错误"（可能漏检）
余数非0 → ______（未检出差错/检测出差错）
```

[ ] 过关

## 5. 交换机MAC表

```
收到帧 → 记录______MAC → 查______MAC
  在表中→ 端口=入口→______
  在表中→ 端口≠入口→______
  不在表中→ ______
```

[ ] 过关

## 6. 停等/GBN/SR 对比（408 必考）

```
               发送窗口    接收窗口    确认方式    重传范围
停等协议：        ____        ____      ________    ________
GBN（后退N）：   ≤ ____      ____      ________    ________
SR（选择重传）：  ≤ ____     ≤ ____     ________    ________

序号位数 n：
  GBN 窗口 ≤ ______
  SR 窗口 ≤ ______

记忆：GBN 一次重传______；SR 只重传______
```

[ ] 过关

## 7. 二进制指数退避

```
第 n 次冲突后：
  k = min(______, ______)
  退避时间 = r × ______，r ∈ [0, ______]

例子：第 3 次冲突 → k=___，r 最大=___，等待最长 = ___×51.2μs
```

[ ] 过关

## 8. CSMA/CA 与 802.11（无线）

```
CSMA/CA 用于______网络（不能边发边听）
信道预约：交换______和______帧（解决______终端问题）
IFS：正常发送涉及的排序（短→长）______ < ______ < ______；
     EIFS（错误接收后）比 DIFS 长，但不参与正常发送比较

CSMA/CD 用于______网络（能______检测冲突）
```

[ ] 过关

## 9. CDMA 码片计算

```
规则：内积 = 收到的序列 × 码片序列，再除以码片长度（归一化）
  内积 > 0 → 该 bit = ____
  内积 < 0 → 该 bit = ____
  内积 = 0 → 该 bit = ____（对方没发）

例：码片 (1,1,1,1)，收到 (2,0,2,0)（A 发 1 + B 发 1 的叠加）→ 内积 = 2×1+0×1+2×1+0×1 = ___
  归一化 = ___/4 = ___（±1 或 0 才是规范值）→ bit=___
```

[ ] 过关

## 10. 时延/利用率计算（408 真题型）

```
τ（单向传播时延）= ______ / ______

最短帧长 = ______ × ______（争用期×速率）

最短检测到冲突 = ___τ；最长检测到冲突 = ___τ
  （τ~2τ 是特定两端题设结论；通用上界是最坏 2τ）

GBN 利用率 = min(1, ______×发送时间 / (发送时间 + ______ + T_ACK))
  ← 封顶 1；题给 ACK 帧长时计入 T_ACK，忽略则为 0

卫星链路（高时延）设计题步骤：
  ① 算______ ② 定______（窗口） ③ 定______位数（GBN窗口≤2ⁿ-1）
```

[ ] 过关
