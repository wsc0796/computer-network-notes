# 移动 IP 专题（解析版）

## 答案

`1. B  2. C  3. A  4. B`

## 解析

1. 归属代理通过“归属地址 -> CoA”绑定找到移动主机当前所在网络。
2. 隧道封装保留原始 IP 分组，并增加外层 IP 首部，使外层分组路由到 CoA。
3. CoA 改变后，旧绑定不再指向当前位置，必须更新登记。
4. 归属地址用于持续标识移动主机；CoA 用于当前可达。隧道封装不会移除原 IP 首部。
