# 应用层：原资料包对照补强

## 1. 已覆盖较好的内容

右侧资料包已经覆盖：

```text
HTTP vs HTTPS
HTTP 无状态
DNS 查询过程
FTP 双连接
C/S、P2P、CDN
SMTP、POP3、IMAP
Cookie、Web缓存、CDN
MIME/base64
```

## 2. 当前薄弱点

在没有课堂截图前，应用层还需要后续确认：

```text
老师是否重点讲 DNS 递归/迭代
老师是否要求画浏览器访问网页全过程
是否考 MIME/base64
是否考 FTP 主动/被动模式
是否考 HTTP 报文格式和状态码
```

## 3. 现阶段补强方向

优先补：

```text
DNS 递归 vs 迭代
FTP 控制连接和数据连接
SMTP/POP3/IMAP 对比
HTTP/HTTPS 对比
Cookie、缓存、CDN 的作用
```

## 4. 后续截图笔记进来后

需要做三件事：

```text
1. 把课堂截图中的应用层小节顺序并入 02_课堂截图对应笔记.md。
2. 把老师反复强调的内容补入本文件。
3. 把新增题点同步到 04_题型训练.md。
```

