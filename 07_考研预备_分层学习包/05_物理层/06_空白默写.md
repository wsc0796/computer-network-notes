# 06_空白默写 · 物理层

## 1. 奈奎斯特 vs 香农

```
奈奎斯特（无噪声）：C = ______
香农（有噪声）：C = ______

dB→S/N：10dB=___  20dB=___  30dB=___  40dB=___
```

[ ] 过关

## 2. 比特率 vs 波特率

```
比特率 = ______ × ______

M=2  → 每码元___bit  → log₂M=___
M=4  → 每码元___bit  → log₂M=___
M=16 → 每码元___bit  → log₂M=___
M=64 → 每码元___bit  → log₂M=___
```

[ ] 过关

## 3. 调制方式

```
ASK → 调制参数：______
FSK → 调制参数：______
PSK → 调制参数：______
QAM → 调制参数：______
```

[ ] 过关

## 4. 复用技术

```
FDM → 按______划分
TDM → 按______划分, 固定时间片
STDM → 数据中加______标识
WDM → 按______划分（光纤）
CDMA → 按______区分
```

[ ] 过关

## 5. 通信方式

```
单工：______方向  例：______
半双工：______方向但不同时  例：______
全双工：______方向同时  例：______
```

[ ] 过关

## 6. 编码识别（408 必考）

```
看波形判断编码：

① 每个位中间有跳变吗？
   没有 → ______ 或 ______
   有   → ______ 或 ______

② 进一步区分：
   NRZ：看______（电平高低映射以题干/题图约定为准，不固定"高=1"）
   NRZ-I：看______（有跳变=1, 无跳变=0）
    曼彻斯特：看______（方向映射以题干/题图约定为准；若题干规定 ↓=1、↑=0，则按该规定）
   差分曼彻斯特：看______（边界有跳=0, 无跳=1）

自检：10Base-T 用______编码，编码效率______；100BASE-TX 使用______编码（完整写作______块编码 + ______线路编码，4B/5B 和 MLT-3 都要写全）
```

[ ] 过关

## 7. 三种交换方式时延（23-25 连续出题）

```
电路交换：
  总时延（忽略传播） = ______（建立） + ______（传输）
  优点：______  缺点：______

报文交换：
  逐跳______转发，整份报文
  总时延（h 条等速链路） = ______ × ______

分组交换：
  分组可______转发（流水线）
  总时延（h 条等速链路） = (______ + ______ - 1) × ______
  三种交换时延大小：必须代入______、______、______和链路数后比较，不背固定排序

存储-转发公式（⚠️ 适用条件：各分组等长，或末分组被填充到完整长度；否则须单独算末分组）：
  每个分组发送时延 = 分组总长度 / ______
  分组数 N = ⌈文件大小 / (分组大小 - ______开销)⌉
  等长时链路实际发送量 = N × ______（含每个分组重复携带的首部）；
  末分组未填满时 = 文件有效数据量 + N × ______
```

[ ] 过关

## 8. 物理层接口四特性

```
① ______特性：接口形状、引脚数目
② ______特性：各引脚电压范围
③ ______特性：引脚的功能（数据/控制/电源）
④ ______特性：事件发生顺序

不属于接口规范范围的是：______（那是链路层的事）
```

[ ] 过关

## 9. 传输介质

```
双绞线：10Base-______（100m）
粗同轴电缆：10Base-______（500m）
细同轴电缆：10Base-______（185m）
光纤：10Base-______，抗干扰强

100Base-T 的 "Base" 表示______传输
```

[ ] 过关

## 10. 综合计算（408 真题型）

```
① 2400bps 四相位调制 → 波特率 = 2400 / log₂(___) = ______

② 带宽 3kHz，4 相位×4 振幅 QAM → V=___，C = 2×3000×log₂(___) = ______bps

③ 带宽 4MHz，最大速率 48Mbps → log₂V = 48M/(2×4M) = ___ → V=___ → QAM-____

④ 无噪声 200kHz 信道，4 幅值 ASK → V=___，C = ______
```

[ ] 过关
